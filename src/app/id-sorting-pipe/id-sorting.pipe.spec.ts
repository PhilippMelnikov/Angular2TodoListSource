import { IdSortingPipe } from './id-sorting.pipe';

describe('IdSortingPipe', () => {
  it('create an instance', () => {
    const pipe = new IdSortingPipe();
    expect(pipe).toBeTruthy();
  });
});
