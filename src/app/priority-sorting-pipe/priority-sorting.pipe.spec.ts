import { PrioritySortingPipe } from './priority-sorting.pipe';

describe('PrioritySortingPipe', () => {
  it('create an instance', () => {
    const pipe = new PrioritySortingPipe();
    expect(pipe).toBeTruthy();
  });
});
