import { StatusSortingPipe } from './status-sorting.pipe';

describe('StatusSortingPipe', () => {
  it('create an instance', () => {
    const pipe = new StatusSortingPipe();
    expect(pipe).toBeTruthy();
  });
});
